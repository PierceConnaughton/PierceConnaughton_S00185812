<Window x:Name="MainWindow1" x:Class="FinalTest.MainWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:FinalTest"
        mc:Ignorable="d"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Title="MainWindow" Height="450" Width="800" Loaded="MainWindow1_Loaded">

    <Window.Resources>
        <DataTemplate x:Key="listboxTemplate">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Image Source="{Binding StudentImage}" Width="75" />

                <StackPanel>
                    <TextBlock Text="{Binding StudentName}" Margin="5" FontSize="12" />
                    <TextBlock Text="{Binding StudentNumber}" Margin="5" FontSize="12" />
                    <TextBlock Text="{Binding GPA}" Margin="5" Background="{Binding GPA_Colour}"  FontSize="12" />
                </StackPanel>

            </StackPanel>

        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ListBox x:Name="lstBx" HorizontalAlignment="Left" Height="223" VerticalAlignment="Top" Width="253" Margin="52,60,0,0"/>

    </Grid>
</Window>
